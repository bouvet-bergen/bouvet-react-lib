import { Demo, Test } from './'

describe('DemoComponent', () => {
  it('is truthy', () => {
    expect(Demo).toBeTruthy()
  })
})

describe('TestComponent', () => {
  it('is truthy', () => {
    expect(Test).toBeTruthy()
  })
})
